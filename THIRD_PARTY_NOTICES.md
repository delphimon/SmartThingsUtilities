# Third-party notices

`src/smartthings_utilities/data/device_catalog.json` is a mechanically generated
index of identifiers and labels from the
[SmartThings Community Edge Drivers](https://github.com/SmartThingsCommunity/SmartThingsEdgeDrivers)
at the exact revision recorded inside the JSON file. That source is licensed under
the Apache License 2.0; a copy is included at `LICENSES/Apache-2.0.txt`.

The generated catalog has been reformatted and reduced to exact matching keys,
device labels, and provenance. It does not include Edge-driver source code.

The same generated catalog contains Z-Wave manufacturer, model, and product
description data from the
[Z-Wave JS configuration database](https://github.com/zwave-js/zwave-js), at
the exact revision recorded in the JSON file. Z-Wave JS is licensed under the
MIT License; a copy is included at `LICENSES/Z-Wave-JS-MIT.txt`.

The reduced firmware compatibility records in the generated catalog come from
the [Z-Wave JS Firmware Update Service](https://github.com/zwave-js/firmware-updates)
at the exact revision recorded in the JSON file. That project is MIT-licensed;
a copy is included at `LICENSES/Z-Wave-Firmware-Updates-MIT.txt`.
